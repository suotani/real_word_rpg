name: Test Branch Input

on:
  workflow_dispatch:
    inputs:
      use-workflow-from:
        default: 'run'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo branch
        run: echo "Branch is ${{ github.event.inputs.branch }}"
