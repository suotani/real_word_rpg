<div class="container">
  <div class="card" style="max-width: 700px; margin: 50px auto">
    <div class="card-header">
      経験
    </div>
    <div class="card-body">
      <%= form_with model: @experience, local: true do |f| %>
        <%= f.hidden_field :charactor_id, value: @experience.charactor_id %>
        <div class="field form-group">
          <label>名前</label><br />
          <%= f.text_field :name, class: "form-control" %>
        </div>
        <div class="field form-group">
          <label>単位</label><br />
          <%= f.select :category_id,Category.all.map{|v| [v.name, v.id]}, {},{class: "form-control"} %>
        </div>
        <div class="field form-group">
          <label>単位</label><br />
          <%= f.select :unit,Experience::UNITS, {},{class: "form-control"} %>
        </div>
        <div class="field form-group">
          <label>取得経験値</label><br />
          <p>１単位当たりの取得経験値を入力して下さい</p>
          <p>100経験値で１レベルアップします</p>
          <%= f.number_field :unit_exp, class: "form-control" %>
        </div>
        <div class="field form-group">
          <label>説明</label><br />
          <%= f.text_area :explanation, row: 4, class: "form-control" %>
        </div>

        <div class="field form-group">
          <input type="submit" value="作成" class="btn btn-primary">
        </div>
      <% end %>
    </div>
  </div>

</div>