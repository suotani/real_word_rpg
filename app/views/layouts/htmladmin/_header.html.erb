<header>
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Xnotepad", "/", class: "navbar-brand" %>
        </div>
        <div class="collapse navbar-collapse" id="navbar-mail">
          <ul class="nav navbar-nav">
            <li class="active">
            </li>
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <li>
              <a href="/#how_to_use_neighborhood">使い方</a>
            </li>
            <li>
              <%= link_to 'マイページ', htmladmin_user_path(current_user) if current_user %>
              <%= link_to 'ユーザー登録', new_user_path unless current_user %>
            </li>
            <% if current_user %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  設定
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to 'ユーザ情報更新', edit_htmladmin_user_path(current_user.id) %></li>
                </ul>
              </li>
            <% else %>
              <li>
                <%= link_to 'ログイン', login_path %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </header>
<% if flash[:notice] %>
  <div class="alert alert-dismissible alert-success" id="notice_message">
    <%= flash[:notice] %>
  </div>
<% end %>
<% if flash[:alert] %>
  <div class="alert alert-dismissible alert-danger" id="alert_message">
    <%= flash[:alert] %>
  </div>
<% end %>